{
  "name": "prettier-plugin-maraca",
  "version": "0.2.1",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "prettier": "prettier --write \"src/**/*.ts\"",
    "build": "rimraf lib/* && tsc",
    "prepare": "yarn run build",
    "test": "yarn build && node lib/test.js"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  },
  "dependencies": {
    "maraca": "^0.4.1",
    "prettier": "^1.16.4"
  },
  "devDependencies": {
    "rimraf": "^2.6.3",
    "typescript": "^3.3.3"
  }
}
